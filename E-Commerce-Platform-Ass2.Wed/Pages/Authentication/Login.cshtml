@page
@model E_Commerce_Platform_Ass2.Wed.Pages.Authentication.LoginModel
@{
    ViewData["Title"] = "Đăng nhập";
    Layout = "~/Pages/Shared/_AuthLayout.cshtml";
}

<div class="text-center mb-4">
    <h2 class="font-weight-semi-bold mb-3">Đăng nhập tài khoản</h2>
    <p class="text-muted">Chào mừng bạn trở lại! Vui lòng đăng nhập vào tài khoản của bạn.</p>
</div>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="fas fa-check-circle mr-2"></i>@TempData["SuccessMessage"]
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
}

<form method="post" class="mt-4">
    <div asp-validation-summary="All" class="alert alert-danger" role="alert"></div>
    
    <div class="form-group">
        <label asp-for="Input.Email" class="font-weight-semi-bold">
            <i class="fas fa-envelope text-primary mr-2"></i>Email
        </label>
        <input asp-for="Input.Email" class="form-control border-0 py-4" placeholder="Nhập email của bạn" />
        <span asp-validation-for="Input.Email" class="text-danger small"></span>
    </div>

    <div class="form-group">
        <label asp-for="Input.Password" class="font-weight-semi-bold">
            <i class="fas fa-lock text-primary mr-2"></i>Mật khẩu
        </label>
        <input asp-for="Input.Password" class="form-control border-0 py-4" placeholder="Nhập mật khẩu của bạn" />
        <span asp-validation-for="Input.Password" class="text-danger small"></span>
    </div>

    <div class="form-group d-flex justify-content-between align-items-center">
        <div class="custom-control custom-checkbox">
            <input asp-for="Input.RememberMe" type="checkbox" class="custom-control-input" id="rememberMe">
            <label class="custom-control-label" for="rememberMe" asp-for="Input.RememberMe"></label>
        </div>
        <a href="#" class="text-primary">Quên mật khẩu?</a>
    </div>

    @if (Model.ShowResendLink)
    {
        <div class="alert alert-warning">
            <i class="fas fa-exclamation-triangle mr-2"></i>
            Email chưa được xác thực. 
            <a asp-page="/Authentication/ResendVerification" asp-route-email="@ViewData["Email"]" class="font-weight-bold">
                Gửi lại email xác thực
            </a>
        </div>
    }

    <div class="form-group mb-4">
        <button type="submit" class="btn btn-primary btn-block border-0 py-3">
            <i class="fas fa-sign-in-alt mr-2"></i>Đăng nhập
        </button>
    </div>
</form>

<div class="text-center">
    <p class="text-muted mb-0">Chưa có tài khoản?</p>
    <a asp-page="/Authentication/Register" class="font-weight-semi-bold text-primary">
        <i class="fas fa-user-plus mr-1"></i>Đăng ký ngay
    </a>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
