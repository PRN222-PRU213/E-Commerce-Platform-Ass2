@page
@model E_Commerce_Platform_Ass2.Wed.Pages.Admin.Categories.IndexModel
@{
    ViewData["Title"] = "Quản lý Danh mục";
    Layout = "~/Pages/Shared/_AdminLayout.cshtml";
}

<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0">
            <i class="fas fa-tags text-primary mr-2"></i>Quản lý Danh mục
        </h2>
        <a asp-page="/Admin/Categories/Create" class="btn btn-primary">
            <i class="fas fa-plus mr-2"></i>Thêm danh mục
        </a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle mr-2"></i>@TempData["Success"]
            <button type="button" class="close" data-dismiss="alert"><span>&times;</span></button>
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="fas fa-exclamation-circle mr-2"></i>@TempData["Error"]
            <button type="button" class="close" data-dismiss="alert"><span>&times;</span></button>
        </div>
    }

    <div class="card border-0 shadow-sm">
        <div class="card-header bg-light py-3">
            <h5 class="mb-0"><i class="fas fa-list mr-2"></i>Danh sách danh mục (@Model.Categories.Count)</h5>
        </div>
        <div class="card-body">
            @if (Model.Categories.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead class="thead-light">
                            <tr>
                                <th>Tên danh mục</th>
                                <th class="text-center">Số sản phẩm</th>
                                <th class="text-center">Trạng thái</th>
                                <th class="text-center">Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var category in Model.Categories)
                            {
                                <tr>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <div class="bg-primary rounded-circle d-flex align-items-center justify-content-center mr-3" style="width: 40px; height: 40px;">
                                                <i class="fas fa-tag text-white"></i>
                                            </div>
                                            <strong>@category.Name</strong>
                                        </div>
                                    </td>
                                    <td class="text-center align-middle">
                                        <div class="d-flex flex-column align-items-center">
                                            <span class="h5 mb-0 font-weight-bold text-primary">@category.ProductCount</span>
                                            <small class="text-muted">sản phẩm</small>
                                        </div>
                                    </td>
                                    <td class="text-center align-middle">
                                        @if (category.Status == "Active")
                                        {
                                            <div class="d-inline-flex align-items-center bg-success text-white rounded-pill px-3 py-2">
                                                <i class="fas fa-check-circle mr-2"></i>
                                                <span class="font-weight-bold">Hoạt động</span>
                                            </div>
                                        }
                                        else
                                        {
                                            <div class="d-inline-flex align-items-center bg-secondary text-white rounded-pill px-3 py-2">
                                                <i class="fas fa-eye-slash mr-2"></i>
                                                <span class="font-weight-bold">Ẩn</span>
                                            </div>
                                        }
                                    </td>
                                    <td class="text-center align-middle">
                                        <a asp-page="/Admin/Categories/Edit" asp-route-id="@category.Id"
                                           class="btn btn-sm btn-outline-primary" title="Chỉnh sửa">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        @if (category.ProductCount == 0)
                                        {
                                            <form asp-page="/Admin/Categories/Delete" asp-route-id="@category.Id"
                                                  method="post" class="d-inline"
                                                  onsubmit="return confirm('Bạn có chắc muốn xóa danh mục này?')">
                                                <button type="submit" class="btn btn-sm btn-outline-danger" title="Xóa">
                                                    <i class="fas fa-trash"></i>
                                                </button>
                                            </form>
                                        }
                                        else
                                        {
                                            <button type="button" class="btn btn-sm btn-outline-secondary" disabled title="Không thể xóa (có sản phẩm)">
                                                <i class="fas fa-trash"></i>
                                            </button>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="text-center py-5">
                    <i class="fas fa-folder-open fa-4x text-muted mb-3"></i>
                    <h5 class="text-muted">Chưa có danh mục nào</h5>
                    <p class="text-muted mb-4">Tạo danh mục đầu tiên để quản lý sản phẩm.</p>
                    <a asp-page="/Admin/Categories/Create" class="btn btn-primary">
                        <i class="fas fa-plus mr-2"></i>Thêm danh mục đầu tiên
                    </a>
                </div>
            }
        </div>
    </div>

    <a asp-page="/Admin/Index" class="btn btn-outline-secondary mt-3">
        <i class="fas fa-arrow-left mr-2"></i>Quay lại Dashboard
    </a>
</div>

