@page
@model E_Commerce_Platform_Ass2.Wed.Pages.Wallet.TopUpModel
@{
    ViewData["Title"] = "Nạp tiền vào ví";
    Layout = "~/Pages/Shared/_EShopperLayout.cshtml";
}

<div class="container mt-4">
    <h3 class="mb-4 text-center">💳 Nạp tiền vào ví</h3>

    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm border-0 rounded-4">
                <div class="card-body p-4">
                    @if (TempData["Error"] != null)
                    {
                        <div class="alert alert-danger">
                            @TempData["Error"]
                        </div>
                    }

                    <form method="post">
                        @Html.AntiForgeryToken()

                        <div class="mb-4">
                            <label asp-for="Input.Amount" class="form-label fw-semibold">Số tiền muốn nạp (VNĐ)</label>
                            <input asp-for="Input.Amount" class="form-control form-control-lg text-center" 
                                   placeholder="Nhập số tiền" />
                            <span asp-validation-for="Input.Amount" class="text-danger"></span>
                            <div class="form-text">Tối thiểu: 10,000 VNĐ - Tối đa: 10,000,000 VNĐ</div>
                        </div>

                        <div class="mb-4">
                            <div class="d-flex flex-wrap gap-2 justify-content-center">
                                <button type="button" class="btn btn-outline-primary quick-amount" data-amount="50000">50,000</button>
                                <button type="button" class="btn btn-outline-primary quick-amount" data-amount="100000">100,000</button>
                                <button type="button" class="btn btn-outline-primary quick-amount" data-amount="200000">200,000</button>
                                <button type="button" class="btn btn-outline-primary quick-amount" data-amount="500000">500,000</button>
                                <button type="button" class="btn btn-outline-primary quick-amount" data-amount="1000000">1,000,000</button>
                            </div>
                        </div>

                        <hr />

                        <div class="alert alert-info small mb-4">
                            <strong>💡 Phương thức thanh toán:</strong> Momo
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary btn-lg">
                                <i class="fas fa-credit-card me-2"></i>Tiếp tục thanh toán
                            </button>
                        </div>

                        <div class="text-center mt-3">
                            <a asp-page="/Wallet/Index" class="text-muted">
                                <i class="fas fa-arrow-left me-1"></i>Quay lại ví
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.querySelectorAll('.quick-amount').forEach(btn => {
            btn.addEventListener('click', function() {
                const amount = this.getAttribute('data-amount');
                document.querySelector('input[name="Input.Amount"]').value = amount;
            });
        });
    </script>
}
